<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userDB">
	<select id="selectUserAll" resultType="UserVO">
		select * from user;
	</select>
	<select id="selectUser" parameterType="string" resultType="UserVO">
		select * from user where id=#{value}
	</select>
	<select id="insertUser">
		insert into user(id,name,password,birth,nickname,email,phone,msg_agree)
		values(#{id},#{name},#{password},'1997-03-07',#{nickname},#{email},#{phone},#{msg_agree});
	</select>
	<select id="boardList" resultType="SellBoardVO">
		select * from product;
	</select>
</mapper>